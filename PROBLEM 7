//A program that generates the sum of all whole numbers starting from 1 up to a given number. 

#include <iostream>
using namespace std;

int main()
{
    int a, sum;

a = 0;
sum = 0;

cout << "Enter a number: ";
    cin >> a;
    
while (a>0)
	{
		for (int b = 1; b <= a; b++) 
   		{
        sum += b;
    	}
	 cout << "The sum of all whole numbers from 1 to "<< a << " is " << sum << "." << endl;
	 cout << "Enter a number: ";
    cin >> a;
    sum = 0;
	}

while(a != 0 && a <= 0)
{

    cout<<"Thank You!";
    break;
}
    return 0;
}
